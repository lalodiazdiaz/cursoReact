const cuenta = -10;

const mensaje = cuenta < 0 ? "Sin saldo" : "Con saldo";

// let mensaje;

// if (cuenta < 0) mensaje = "No tienes saldo";
// else mensaje = "tienes saldo";

document.write(mensaje);
